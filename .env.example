# crystelf 后端服务地址
# 本地开发指向插件运行的 HTTP 服务，例如 http://localhost:7345
NEXT_PUBLIC_API_BASE=http://localhost:7345

# 可选：服务端代理地址（留空则直接走 NEXT_PUBLIC_API_BASE）
# 设置后 Server Component 会走这个地址，避免公网暴露
API_INTERNAL=
